using System;

namespace Muse.SourceLink.Library
{
    public class Messenger
    {
        public string GenerateMessage(string source)
        {
            return $"Hello world from {source}!";
        }
    }
}
